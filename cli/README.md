# Frameplay CLI

## Install

```
$ cargo install frameplay-cli --locked
```

## Usage

Write frame file in the following format:

```json
{
  "frames": ["\\", "|", "/", "-"]
}
```

Then run

```
$ frameplay-cli <path to your file>
```
